[package]
name = "loan_core"
version = "0.0.1"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]
name = "loan_core"

[dependencies]
hdk = "0.2.1"
serde = { version = "1.0", features = ["derive"] }
